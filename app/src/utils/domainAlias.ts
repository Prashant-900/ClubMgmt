/**
 * "Club → Domain" display alias.
 *
 * The backend and all API params/types use "club" (clubId, Club, etc.). The
 * product surfaces it to users as "Domain" — matching the web frontend. These
 * constants are for user-facing copy ONLY; never use them for API params,
 * route names, or type names.
 */
export const DOMAIN_LABEL = 'Domain';
export const DOMAIN_LABEL_PLURAL = 'Domains';
export const DOMAIN_LABEL_LOWER = 'domain';
export const DOMAIN_LABEL_LOWER_PLURAL = 'domains';
